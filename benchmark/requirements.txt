# negotiation simulation / LLM calls
openai
python-dotenv
tqdm

# data analysis / visualization (analysis.ipynb) -- viz standardized on seaborn (matplotlib-based)
pandas
numpy
matplotlib
seaborn

# (legacy) defect-synthesis image dataset generation -- data/dataset_v1/{pilot,full_run}/*.py, already run
Pillow
google-genai

# Jupyter runtime -- remaining transitive deps (ipykernel/notebook/jsonschema/...) are pulled in by pip
jupyter
